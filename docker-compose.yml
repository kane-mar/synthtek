# Base compose file for synthtek
# Merge with dev/prod overrides as needed:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up

services:
  synthtek:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synthtek
    restart: unless-stopped

    # Volume mounts
    volumes:
      - ./config:/data/config
      - ./data:/data
      - ./logs:/data/logs

    # Environment variables
    environment:
      - NODE_ENV=production
      - SYNTHTEK_WORKSPACE=/data
      - LOG_LEVEL=info

    # Health check (ESM-compatible)
    healthcheck:
      test: ["CMD", "node", "-e", "import('fs').then(fs => process.exit(fs.existsSync('/app/dist/src/cli.js') ? 0 : 1))"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

    # Resource limits
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 128M
          cpus: "0.25"

    # Non-root user (matches Dockerfile)
    user: "1000:1000"
